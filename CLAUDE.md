# CLAUDE.md — sofon-site

Контекст для продовження роботи над сайтом **Sofon** у будь-якій сесії (Claude Code / Cowork).
Прочитай цей файл першим.

## Що це
Сайт компанії **Sofon** (ТОВ «СОФОН») — інженерно-виробничий завод обладнання для ліній
розливу напоїв і води. **Статичний сайт: голий HTML/CSS, без CMS, фреймворків і бекенду.**
Реальний бойовий проєкт, не тренувальний.

- Продакшн: **https://vizitka-site-eight.vercel.app** (Vercel, авто-деплой при пуші в `main`).
- Мови: **UA — основна** (корінь), **EN — дзеркало під `/en/`** (окремі файли, без JS-перемикання).

## Позиціонування (ВАЖЛИВО — пивот 22-07-2026)
- **Флагман — Оснастка** (форматні деталі для переведення лінії розливу на нову пляшку):
  зірки, ліри напрямні, шнеки поділу потоку, турнікетні групи, системи роботи з пробкою,
  переносники етикетки.
- **Палетизатор 19 л** (1400 пляшок/год) — більше НЕ флагман, це кейс усередині
  «Нестандартного обладнання».
- **Контрактне виробництво** (токарні/фрезерні на замовлення) — окрема лінія, інша
  аудиторія, окремий CTA «Розмістити замовлення» (не загальний «Отримати прорахунок»).
- Референси ніші: arol.com, khs.com, kosme.com. Тон: інженерний, точний, надійний,
  сучасно-промисловий. НЕ дешевий/крикливий/consumer.

## Дизайн-система
Джерело правди — `docs/brand-tokens.md`, реалізовано в `css/styles.css`. Ключове:
- Акцент: **Sofon Amber `#F07C1A`** (дефолт). Альтернатива — Engineering Blue `#1466D8`.
- Графіт `#1B1E23` (текст), Steel `#5D6772` (підписи), Mist `#F3F4F6` (фон секцій),
  Border `#D9DDE2`, темний футер `#12151A`.
- Шрифти: **IBM Plex Sans** (400/500/600/700) — текст/заголовки; **IBM Plex Mono**
  (400/500/600) — лейбли/eyebrow/спеки (клас `.mono`, `.eyebrow`).
- Сітка: 12 колонок, max-width **1120px**, поля 56px desktop / 24px mobile, gutters 24px.
- Радіус: sharp **2px** (дефолт) / soft **8px**. Spacing: 4·8·16·24·32·48·64.
- Готові класи: `.btn`(--primary/--ghost/--onDark/--lg), `.section`(--mist/--dark),
  `.container`, `.eyebrow`, `.lead`, `.card`(--flag), `.grid`(--2/--3), `.stats`, `.spot`,
  `.band`, `.feat`, `.spec`, `.review`, `.crumb`, `.ctype`, `.slot`.
- **Шапка внутрішньої сторінки** — `.phead` у двох прийомах: `--sheet` «кресленик»
  (креслярська сітка + янтарне світло + деталь просто на сітці + штамп унизу) і
  `--bleed` «виріз» (чисте тло, кадр на повний виліт до краю екрана). Опис, заміри
  й пастки: `docs/page-header-variants.md`. Штамп `.stamp` — окремий компонент,
  доречний і поза шапкою. Темний `.phero` лишився тільки на
  `/products/custom-equipment/`. Внутрішня сторінка закінчується смугою `.endcta`.
- **Галерея зображень** — постійний елемент дизайну, окремий файл `css/gallery.css`
  (+ `js/gallery.js` лише для варіанта-стрічки). Розкладки й де який доречний:
  `docs/gallery-variants.md`. Розмітка галереї у сторінках СТАТИЧНА — без JS вона
  теж працює й індексується.
- **Шапка** (27-07): фон — два напівпрозорі шари, які переливаються за числом
  `--np` (0 над героєм → 1 нижче, рахує `js/site.js`); над темним героєм шапка
  темна, нижче світла. «Продукти» має панель на дві колонки; окремого пункту
  «Контакти» немає — його роль виконує кнопка «Зв'язатися»: клік веде на
  `/contact/`, наведення розкриває швидкі канали.

## Архітектура файлів і як редагувати
**Шапка й футер спільні для всіх сторінок. НЕ дублюй їх руками — вони збираються скриптом.**

- `_src/header.html`, `_src/footer.html` — партіали спільної оболонки (джерело правди).
  Плейсхолдери в header: `__TITLE__`, `__DESC__`, `__PAGE__` (id для active-стану nav).
- `_src/pages/<name>.html` — ТІЛЬКИ вміст `<main>` конкретної сторінки (без шапки/футера).
- `build.sh` — асемблер: `header + _src/pages/<body> + footer` → фінальні `*.html`.
  Манифест сторінок (шлях|id|body|title|desc) — усередині `build.sh`, масив `PAGES`.
- Згенеровані `index.html`, `products/**/index.html` тощо мають маркер «⚠ ГЕНЕРУЄТЬСЯ» —
  їх НЕ редагувати напряму.

### Додати нову сторінку
1. `_src/pages/<name>.html` — верстка вмісту `<main>` (використовуй наявні класи вище).
2. Додай рядок у масив `PAGES` в `build.sh`: `шлях|page_id|body|<title>|<meta desc>`.
3. Active-стан у nav: додай пару в CSS-правило `body[data-page="..."] [data-nav="..."]`
   (кінець `css/styles.css`) і `data-nav` на відповідне посилання в `_src/header.html`.
4. Запусти `./build.sh`, перевір результат, закоміть і запуш.

Шляхи в посиланнях/ассетах — **кореневі-абсолютні** (`/css/styles.css`, `/products/osnastka/`),
щоб працювали з будь-якої вкладеності. Vercel віддає з кореня репо.

## Статус (27-07-2026)
Готово (UA): design-system, шапка/футер, галерея-компонент, сторінки `/`,
`/products/osnastka/`, `/contract-manufacturing/` (🟡 ЧЕРНЕТКА: курсивні плейсхолдери
фактів + плашка `.review` + три помічені слоти під фото цеху, яких ще немає; чекає від
власника парк верстатів, макс. габарити, матеріали, терміни, мін. замовлення),
`/contact/`, `/products/custom-equipment/`, `/products/conveyor-systems/`.

**Головна перероблена (27-07):** повноекранний герой-спліт (теза ліворуч, «Чому Sofon»
втягнуто в герой праворуч), покажчик шести напрямів зі прев'ю, секція оснастки з
галереєю-мозаїкою, смуга контрактного виробництва. Дизайн вироблено в прототипах
`home-ab/` — вони лишаються як історія рішень, але **джерело правди тепер `_src/`**;
правити треба там, прототипи більше не збігаються з сайтом.

**Внутрішні сторінки (25-07):** вироблено шапку `.phead` замість темного `.phero`.
Перша пара варіантів («реєстр» і «кадр») відхилена власником: обидва були однією
сіткою з різним наповненням і без фактури. Замінено на два різні прийоми —
«кресленик» (`/products/osnastka/`, `/contract-manufacturing/`) і «виріз»
(`/products/conveyor-systems/`). Деталі й заміри — `docs/page-header-variants.md`.
Додано `/products/conveyor-systems/` (нова сторінка) і галерею-мозаїку на оснастку.

⚠ **Текст конвеєрів — зі старого сайту ЯК Є, російською.** Повна UA-перезапис і
EN-дзеркало відкладені свідомо; українською там лише оболонка (крихти, надзаголовки,
кнопки) — так само, як на `/products/custom-equipment/`.

**Наступний крок:** замінити `.phero` на `/products/custom-equipment/` (єдина сторінка,
що лишилась зі старою шапкою), далі решта продуктових сторінок + `/products/` hub.

Далі (див. `docs/sitemap.md`): решта продуктових сторінок + `/products/` hub + About/News/
Contact (обрати механізм форми: mailto/Formspree/Getform) → повне дзеркало EN під `/en/` →
favicon/OG + реальні фото машин замість плейсхолдер-блоків.

Повний план айдентики й сайту (8 фаз, статус, відкриті питання) — тепер у репо:
`docs/PLAN_brand-identity-sofon.md`. Джерело — Claude Design проєкт «Sofon brand
guidelines» (там і оригінал, і сам brand-guide `Sofon-Brand-Guidelines-v1.0.html`).

## Git і деплой

**Коміт і пуш — різні речі.** Коміт — точка збереження локально, її ніхто не бачить
і сайт від неї не змінюється. Пуш у `main` — публікація: Vercel пересобирає адресу
за секунди.

**Ритм комітів (домовлено 27-07-2026).** Коміт на кожен закінчений змістовний крок,
а не за часом. Один коміт — одна думка. Повідомлення відповідає на «що змінилось і
чому», звичайною мовою: власник читатиме саме його, а не діф. Незакінчене комітити
не страшно — точка збереження на півдорозі краща за її відсутність.

Приклад того, як буває інакше: до 27-07 чотири доби роботи пролежали без жодного
коміту, і весь цей час скрипти правили файли на місці без можливості відкату.

**Пуш дозволено без окремого питання.** `vizitka-site-eight.vercel.app` — тимчасовий
домен розробки, не продакшн; обмеження тільки здоровий глузд (рішення власника
27-07-2026). Вхід у GitHub зберігає диспетчер облікових даних Windows, тож пуш іде
без діалогів і без токена в командному рядку.

Якщо діалог входу все-таки з'явиться — **входить власник, не я** (облікові дані я не
вводжу), а сам пуш треба запускати у фоні або з довгим лімітом: коротким таймаутом
я вже одного разу вбив процес git, і вікно входу закрилось у власника перед носом.

**Токен у репозиторій не потрапляє ніколи.** `.gitignore` тримає `*.bak`, сміття
Windows, кеш Python і шаблони імен для будь-яких майбутніх файлів із ключами.

## Правила
- Парсер не вгадує: якщо факти по продукту невідомі/застаріли — постав плейсхолдер із
  плашкою `.review` і спитай власника, а не додумуй (як зроблено на contract-manufacturing).
- Тексти сайту — UA (укр. мова, не переклад старого RU). Технічні коментарі в коді — вільно.
